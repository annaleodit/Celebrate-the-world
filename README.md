# Celebrate the world bot

Telegram-бот для создания поздравительных открыток с учетом культурных особенностей разных стран.

## 🚀 Быстрый старт

### 1. Установка зависимостей

Зависимости уже установлены. Если нужно переустановить:

```bash
pip3 install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
BOT_TOKEN=your_telegram_bot_token_here
GOOGLE_API_KEY=your_google_api_key_here
ADMIN_ID=your_telegram_user_id_here
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
```

**Где получить:**

- **BOT_TOKEN**: Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
- **GOOGLE_API_KEY**: Получите API ключ в [Google AI Studio](https://makersuite.google.com/app/apikey)
- **ADMIN_ID**: Ваш Telegram User ID (можно узнать через [@userinfobot](https://t.me/userinfobot))

### 3. Запуск бота

**Вариант 1: Использование скрипта (рекомендуется)**

```bash
chmod +x start_bot.sh
./start_bot.sh
```

**Вариант 2: Прямой запуск**

```bash
python3 bot.py
```

### 4. Остановка бота

```bash
chmod +x stop_bot.sh
./stop_bot.sh
```

## 📋 Требования

- Python 3.8+
- Telegram Bot Token
- Google Gemini API Key
- PostgreSQL база данных (для локальной разработки нужен локальный PostgreSQL или используйте Render)
- Шрифт `CinzelDecorative-Regular.ttf` (уже включен в проект)

## 📁 Структура проекта

- `bot.py` - основной файл бота
- `config.py` - конфигурация и переменные окружения
- `text_content.py` - тексты, промпты и логика для разных стран
- `ai_service.py` - работа с Google Gemini API и генерация изображений
- База данных PostgreSQL - данные пользователей хранятся в PostgreSQL (настраивается через DATABASE_URL)

## 🌍 Поддерживаемые страны

- 🇦🇪 ОАЭ
- 🇸🇦 Саудовская Аравия
- 🇮🇳 Индия
- 🇨🇳 Китай

## ⚙️ Команды бота

### Для всех пользователей:
- `/start` - начать создание открытки

### Команды администратора:
- `/admin` или `/help_admin` - показать меню всех админских команд
- `/stats` - расширенная статистика пользователей (всего, новых за день/неделю/месяц)
- `/users` - выгрузка CSV файла со всеми пользователями
- `/user_info <user_id>` - детальная информация о конкретном пользователе
- `/delete_user <user_id>` - удалить пользователя из базы данных
- `/broadcast <сообщение>` - рассылка сообщений всем пользователям
- `/db_stats` - подробная статистика базы данных

**Примечание:** Все админские команды доступны только пользователю с ID, указанным в переменной окружения `ADMIN_ID`.

## 🔧 Устранение неполадок

**Бот не запускается:**
- Проверьте, что файл `.env` создан и содержит все необходимые переменные
- Убедитесь, что все зависимости установлены: `pip3 install -r requirements.txt`
- Проверьте логи: `tail -f bot.log`

**Ошибка генерации изображений:**
- Проверьте валидность `GOOGLE_API_KEY`
- Убедитесь, что у вас есть доступ к Google Gemini API
- Проверьте интернет-соединение

## 📝 Примечания

- Бот использует Google Gemini для генерации изображений
- База данных PostgreSQL создается автоматически при первом запуске
- Данные пользователей сохраняются в PostgreSQL (на Render используется отдельная PostgreSQL база)
- **Данные сохраняются при обновлении бота**, так как база данных отделена от кода бота
- Логи сохраняются в файл `bot.log`

## 🚀 Деплой на Render

### Подготовка к деплою

1. **Создайте репозиторий на GitHub** и загрузите код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/celebrate-the-world-bot.git
   git branch -M main
   git push -u origin main
   ```

2. **Важно**: Убедитесь, что файл `.env` **НЕ** добавлен в репозиторий (он уже в `.gitignore`)

### Деплой на Render

1. Зайдите на [Render Dashboard](https://dashboard.render.com)
2. Нажмите **New** → **Background Worker**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: `celebrate-the-world-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`
   - **Plan**: Выберите подходящий план (Free tier доступен)

5. **Создайте PostgreSQL базу данных**:
   - Нажмите **New** → **PostgreSQL**
   - Выберите план (Free tier доступен)
   - Запомните имя базы данных (оно будет использовано в `render.yaml`)

6. **Добавьте переменные окружения** в разделе **Environment Variables** (для Worker сервиса):
   ```
   BOT_TOKEN = ваш_реальный_токен_бота
   GOOGLE_API_KEY = ваш_реальный_google_api_ключ
   ADMIN_ID = ваш_telegram_user_id
   DEBUG_LOG_ENABLED = false
   ```
   
   ⚠️ **Важно**: 
   - Не добавляйте кавычки вокруг значений!
   - Переменная `DATABASE_URL` будет автоматически добавлена из PostgreSQL сервиса через `render.yaml`

6. Нажмите **Create Background Worker**

7. Render автоматически начнет деплой. Проверьте логи в Dashboard.

### Альтернативный способ: Использование render.yaml

Если вы используете файл `render.yaml`, Render автоматически подхватит настройки:

1. Создайте сервис через **New** → **Blueprint**
2. Подключите репозиторий
3. Render автоматически использует `render.yaml` для настройки
4. PostgreSQL база данных и переменная `DATABASE_URL` будут настроены автоматически

### Проверка работы после деплоя

1. Проверьте логи в Render Dashboard - должно быть сообщение: `🚀 Бот запущен и готов к работе`
2. Откройте бота в Telegram и отправьте `/start`
3. Если есть ошибки, проверьте логи в Render Dashboard

## 🔒 Безопасность

- ✅ Файл `.env` исключен из репозитория через `.gitignore`
- ✅ Все секретные ключи хранятся только в переменных окружения Render
- ✅ Используйте `.env.example` как шаблон для других разработчиков